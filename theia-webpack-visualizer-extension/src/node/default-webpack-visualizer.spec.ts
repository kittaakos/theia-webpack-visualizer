import 'mocha';
import { expect } from 'chai';

describe('webpack-visualizer', () => {

    it('generate', () => {
        expect(true).to.be.true;
    });

    it('bumm!', () => {
        expect(true).to.be.true;
    });

});
